% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fct_isotopes.R
\name{convolve_patterns}
\alias{convolve_patterns}
\title{Convolve two isotope patterns}
\usage{
convolve_patterns(pattern_a, pattern_b, min_prob = 1e-10)
}
\arguments{
\item{pattern_a}{A list of isotope peaks (each a named list with `mass`,
`prob`, and `isotope_counts`).}

\item{pattern_b}{A list of isotope peaks in the same format as `pattern_a`.}

\item{min_prob}{A numeric threshold. Combined peaks with probability below
this value are excluded.}
}
\value{
A list of combined isotope peaks in the same format as the inputs.
}
\description{
Combines two isotope peak lists by convolving them: every peak from
`pattern_a` is paired with every peak from `pattern_b`. Resulting peak
masses are summed and probabilities are multiplied. Pairs whose combined
probability falls below `min_prob` are discarded.
}
